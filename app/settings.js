/**
 * Setup the game settings
 * @param {Number} gridRows numbers of rows in game
 * @param {Number} gridCols numbers of cols in game
 * @param {Array} ships each element is a ship and the number represent the length of it
 */

module.exports = {
  gridRows: 10,
  gridCols: 10,
  ships: [5, 4, 3, 3, 2, 2]
};
